#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    int l = 1;
    int r = 3;
    for (auto x: {l, r}) {
        cout << x;
    }
    return 0;
}