#include <bits/stdc++.h>

using ll = long long;
using namespace std;

void solve() {
    int n, k, l, r;
    cin >> n >> k >> l >> r;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; ++i) cin >> a[i];
    map<int, int> mp;
    
    return;
}


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    int _;
    cin >> _;
    while (_--) {
        solve();
    }
}