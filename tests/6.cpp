#include <bits/stdc++.h>

using namespace std;


void solve() {
    for (int i = 1; i <= 30; i++) {
        cout << 1 << " ";
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int t = 1;
    //cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}