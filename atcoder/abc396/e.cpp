#include <bits/stdc++.h>

using namespace std;
vector<int> edge[200005];
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);
    int n, m;
    cin >> n >> m;
    vector<int> x(m + 1);
    vector<int> y(m + 1);
    vector<int> z(m + 1);
    for (int i = 1; i <= m; i++) {
        cin >> x[i] >> y[i] >> z[i];
    }
    while(m--) {
        
    }
    return 0;
}